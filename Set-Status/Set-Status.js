bot.on ("ready",fonction => {
  bot.user.setStatus("placeholder_status");
  bot.user.setActivity("placeholder_text");
  return (console.error);
});

//Comments
//You can switch between 4 types of status (online -> online | idle -> idle | dnd -> dnd | offline -> offline)
//Placeholders must be changed
